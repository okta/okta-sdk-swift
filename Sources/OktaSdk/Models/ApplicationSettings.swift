//
// ApplicationSettings.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import AnyCodable

public final class ApplicationSettings: Codable, Hashable {

    public var app: AnyCodable?
    public var implicitAssignment: Bool?
    public var inlineHookId: String?
    public var notifications: ApplicationSettingsNotifications?

    public init(app: AnyCodable? = nil, implicitAssignment: Bool? = nil, inlineHookId: String? = nil, notifications: ApplicationSettingsNotifications? = nil) {
        self.app = app
        self.implicitAssignment = implicitAssignment
        self.inlineHookId = inlineHookId
        self.notifications = notifications
    }
    public enum CodingKeys: String, CodingKey, CaseIterable {
        case app
        case implicitAssignment
        case inlineHookId
        case notifications
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(app, forKey: .app)
        try container.encodeIfPresent(implicitAssignment, forKey: .implicitAssignment)
        try container.encodeIfPresent(inlineHookId, forKey: .inlineHookId)
        try container.encodeIfPresent(notifications, forKey: .notifications)
    }



    public static func == (lhs: ApplicationSettings, rhs: ApplicationSettings) -> Bool {
        lhs.app == rhs.app &&
        lhs.implicitAssignment == rhs.implicitAssignment &&
        lhs.inlineHookId == rhs.inlineHookId &&
        lhs.notifications == rhs.notifications
        
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(app?.hashValue)
        hasher.combine(implicitAssignment?.hashValue)
        hasher.combine(inlineHookId?.hashValue)
        hasher.combine(notifications?.hashValue)
        
    }

}
