//
// NetworkZoneAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

open class NetworkZoneAPI {
    /**
     Activate Network Zone
     
     - parameter zoneId: (path)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    open class func activateNetworkZone(zoneId: String, apiResponseQueue: DispatchQueue = OpenAPIClientAPI.apiResponseQueue, completion: @escaping ((_ data: NetworkZone?, _ error: Error?) -> Void)) {
        activateNetworkZoneWithRequestBuilder(zoneId: zoneId).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Activate Network Zone
     - POST /api/v1/zones/{zoneId}/lifecycle/activate
     - Activate Network Zone
     - API Key:
       - type: apiKey Authorization 
       - name: api_token
     - parameter zoneId: (path)  
     - returns: RequestBuilder<NetworkZone> 
     */
    open class func activateNetworkZoneWithRequestBuilder(zoneId: String) -> RequestBuilder<NetworkZone> {
        var path = "/api/v1/zones/{zoneId}/lifecycle/activate"
        let zoneIdPreEscape = "\(APIHelper.mapValueToPathItem(zoneId))"
        let zoneIdPostEscape = zoneIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{zoneId}", with: zoneIdPostEscape, options: .literal, range: nil)
        let URLString = OpenAPIClientAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<NetworkZone>.Type = OpenAPIClientAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Add Network Zone
     
     - parameter zone: (body)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    open class func createNetworkZone(zone: NetworkZone, apiResponseQueue: DispatchQueue = OpenAPIClientAPI.apiResponseQueue, completion: @escaping ((_ data: NetworkZone?, _ error: Error?) -> Void)) {
        createNetworkZoneWithRequestBuilder(zone: zone).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Add Network Zone
     - POST /api/v1/zones
     - Adds a new network zone to your Okta organization.
     - API Key:
       - type: apiKey Authorization 
       - name: api_token
     - parameter zone: (body)  
     - returns: RequestBuilder<NetworkZone> 
     */
    open class func createNetworkZoneWithRequestBuilder(zone: NetworkZone) -> RequestBuilder<NetworkZone> {
        let path = "/api/v1/zones"
        let URLString = OpenAPIClientAPI.basePath + path
        let parameters = JSONEncodingHelper.encodingParameters(forEncodableObject: zone)

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<NetworkZone>.Type = OpenAPIClientAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Deactivate Network Zone
     
     - parameter zoneId: (path)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    open class func deactivateNetworkZone(zoneId: String, apiResponseQueue: DispatchQueue = OpenAPIClientAPI.apiResponseQueue, completion: @escaping ((_ data: NetworkZone?, _ error: Error?) -> Void)) {
        deactivateNetworkZoneWithRequestBuilder(zoneId: zoneId).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Deactivate Network Zone
     - POST /api/v1/zones/{zoneId}/lifecycle/deactivate
     - Deactivates a network zone.
     - API Key:
       - type: apiKey Authorization 
       - name: api_token
     - parameter zoneId: (path)  
     - returns: RequestBuilder<NetworkZone> 
     */
    open class func deactivateNetworkZoneWithRequestBuilder(zoneId: String) -> RequestBuilder<NetworkZone> {
        var path = "/api/v1/zones/{zoneId}/lifecycle/deactivate"
        let zoneIdPreEscape = "\(APIHelper.mapValueToPathItem(zoneId))"
        let zoneIdPostEscape = zoneIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{zoneId}", with: zoneIdPostEscape, options: .literal, range: nil)
        let URLString = OpenAPIClientAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<NetworkZone>.Type = OpenAPIClientAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Delete Network Zone
     
     - parameter zoneId: (path)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    open class func deleteNetworkZone(zoneId: String, apiResponseQueue: DispatchQueue = OpenAPIClientAPI.apiResponseQueue, completion: @escaping ((_ data: Void?, _ error: Error?) -> Void)) {
        deleteNetworkZoneWithRequestBuilder(zoneId: zoneId).execute(apiResponseQueue) { result -> Void in
            switch result {
            case .success:
                completion((), nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Delete Network Zone
     - DELETE /api/v1/zones/{zoneId}
     - Removes network zone.
     - API Key:
       - type: apiKey Authorization 
       - name: api_token
     - parameter zoneId: (path)  
     - returns: RequestBuilder<Void> 
     */
    open class func deleteNetworkZoneWithRequestBuilder(zoneId: String) -> RequestBuilder<Void> {
        var path = "/api/v1/zones/{zoneId}"
        let zoneIdPreEscape = "\(APIHelper.mapValueToPathItem(zoneId))"
        let zoneIdPostEscape = zoneIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{zoneId}", with: zoneIdPostEscape, options: .literal, range: nil)
        let URLString = OpenAPIClientAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<Void>.Type = OpenAPIClientAPI.requestBuilderFactory.getNonDecodableBuilder()

        return requestBuilder.init(method: "DELETE", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Get Network Zone
     
     - parameter zoneId: (path)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    open class func getNetworkZone(zoneId: String, apiResponseQueue: DispatchQueue = OpenAPIClientAPI.apiResponseQueue, completion: @escaping ((_ data: NetworkZone?, _ error: Error?) -> Void)) {
        getNetworkZoneWithRequestBuilder(zoneId: zoneId).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Get Network Zone
     - GET /api/v1/zones/{zoneId}
     - Fetches a network zone from your Okta organization by `id`.
     - API Key:
       - type: apiKey Authorization 
       - name: api_token
     - parameter zoneId: (path)  
     - returns: RequestBuilder<NetworkZone> 
     */
    open class func getNetworkZoneWithRequestBuilder(zoneId: String) -> RequestBuilder<NetworkZone> {
        var path = "/api/v1/zones/{zoneId}"
        let zoneIdPreEscape = "\(APIHelper.mapValueToPathItem(zoneId))"
        let zoneIdPostEscape = zoneIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{zoneId}", with: zoneIdPostEscape, options: .literal, range: nil)
        let URLString = OpenAPIClientAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<NetworkZone>.Type = OpenAPIClientAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     List Network Zones
     
     - parameter after: (query) Specifies the pagination cursor for the next page of network zones (optional)
     - parameter limit: (query) Specifies the number of results for a page (optional, default to -1)
     - parameter filter: (query) Filters zones by usage or id expression (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    open class func listNetworkZones(after: String? = nil, limit: Int? = nil, filter: String? = nil, apiResponseQueue: DispatchQueue = OpenAPIClientAPI.apiResponseQueue, completion: @escaping ((_ data: [NetworkZone]?, _ error: Error?) -> Void)) {
        listNetworkZonesWithRequestBuilder(after: after, limit: limit, filter: filter).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     List Network Zones
     - GET /api/v1/zones
     - Enumerates network zones added to your organization with pagination. A subset of zones can be returned that match a supported filter expression or query.
     - API Key:
       - type: apiKey Authorization 
       - name: api_token
     - parameter after: (query) Specifies the pagination cursor for the next page of network zones (optional)
     - parameter limit: (query) Specifies the number of results for a page (optional, default to -1)
     - parameter filter: (query) Filters zones by usage or id expression (optional)
     - returns: RequestBuilder<[NetworkZone]> 
     */
    open class func listNetworkZonesWithRequestBuilder(after: String? = nil, limit: Int? = nil, filter: String? = nil) -> RequestBuilder<[NetworkZone]> {
        let path = "/api/v1/zones"
        let URLString = OpenAPIClientAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "after": after?.encodeToJSON(),
            "limit": limit?.encodeToJSON(),
            "filter": filter?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<[NetworkZone]>.Type = OpenAPIClientAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Update Network Zone
     
     - parameter zoneId: (path)  
     - parameter zone: (body)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    open class func updateNetworkZone(zoneId: String, zone: NetworkZone, apiResponseQueue: DispatchQueue = OpenAPIClientAPI.apiResponseQueue, completion: @escaping ((_ data: NetworkZone?, _ error: Error?) -> Void)) {
        updateNetworkZoneWithRequestBuilder(zoneId: zoneId, zone: zone).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Update Network Zone
     - PUT /api/v1/zones/{zoneId}
     - Updates a network zone in your organization.
     - API Key:
       - type: apiKey Authorization 
       - name: api_token
     - parameter zoneId: (path)  
     - parameter zone: (body)  
     - returns: RequestBuilder<NetworkZone> 
     */
    open class func updateNetworkZoneWithRequestBuilder(zoneId: String, zone: NetworkZone) -> RequestBuilder<NetworkZone> {
        var path = "/api/v1/zones/{zoneId}"
        let zoneIdPreEscape = "\(APIHelper.mapValueToPathItem(zoneId))"
        let zoneIdPostEscape = zoneIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{zoneId}", with: zoneIdPostEscape, options: .literal, range: nil)
        let URLString = OpenAPIClientAPI.basePath + path
        let parameters = JSONEncodingHelper.encodingParameters(forEncodableObject: zone)

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<NetworkZone>.Type = OpenAPIClientAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "PUT", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

}
